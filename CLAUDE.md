# Claude Code Configuration: cobol-legacy-ledger

## Project Overview

Educational COBOL teaching resource — a fully functional inter-bank settlement system demonstrating COBOL to software engineers. Non-invasive cryptographic integrity layer wraps unmodified COBOL with Python observation and SHA-256 hash chain verification. 6 nodes (5 banks + clearing house).

**Narrative**: "COBOL isn't the problem. Lack of observability is."

---

## Project Purpose

This is a **teaching resource** for IT classes. Every COBOL and Python source file contains thorough educational comments explaining syntax, patterns, and design decisions. The documentation serves as a manual, glossary, and textbook.

---

## Critical Principles

1. **COBOL Immutability** — Wrap, don't modify. Legacy systems unchanged.
2. **Cryptographic Integrity** — SHA-256 chain + HMAC. Detect tampering in <100ms.
3. **Per-Node Database** — Each node has its own SQLite DB. No shared ledger.
4. **6-Node Architecture** — Fixed: BANK_A, BANK_B, BANK_C, BANK_D, BANK_E, CLEARING.
5. **Production COBOL** — Headers + KNOWN_ISSUES.md documentation required.
6. **Testability** — Every requirement must be testable. 280 tests, all green.
7. **No Node.js** — Static HTML/CSS/JS only. No npm, no build process. Web console served via FastAPI StaticFiles at `/console/`.
8. **Clear Error Paths** — Status codes: 00=success, 01=NSF, 02=limit, 03=invalid, 04=frozen, 99=error.
9. **Educational Comments** — Every source file teaches concepts inline with `COBOL CONCEPT:` blocks. Layer 3 (API/LLM) files follow the same standard: 20-40 line module docstrings, `# ── Title ─────────` section banners, class/method docstrings, and inline comments on non-obvious lines.

---

## Key Files & Locations

### COBOL-BANKING (standalone, no Python dependency)

**Source** (10 programs in `COBOL-BANKING/src/`):
- `ACCOUNTS.cob` — Account lifecycle (CREATE, READ, UPDATE, CLOSE, LIST)
- `TRANSACT.cob` — Transaction engine (DEPOSIT, WITHDRAW, TRANSFER, BATCH)
- `VALIDATE.cob` — Business rules (status, balance, limits)
- `REPORTS.cob` — Reporting (STATEMENT, LEDGER, EOD, AUDIT)
- `INTEREST.cob` — Monthly interest accrual (COMPUTE, ROUNDED)
- `FEES.cob` — Monthly maintenance fee processing
- `RECONCILE.cob` — Transaction-to-balance reconciliation
- `SIMULATE.cob` — Deterministic daily transaction generator
- `SETTLE.cob` — 3-leg inter-bank clearing house settlement
- `SMOKETEST.cob` — Compilation verification (START HERE for learning)

**Copybooks** (5 files in `COBOL-BANKING/copybooks/`):
- `ACCTREC.cpy` — Account record layout (70 bytes)
- `TRANSREC.cpy` — Transaction record layout (103 bytes)
- `COMCODE.cpy` — Status codes, bank IDs, constants
- `ACCTIO.cpy` — Shared account I/O table (OCCURS clause)
- `SIMREC.cpy` — Simulation parameters (REDEFINES clause)

**Docs**:
- `COBOL-BANKING/KNOWN_ISSUES.md` — All bugs documented with production fixes
- `COBOL-BANKING/README.md` — Standalone COBOL system overview

**Binary** (gitignored):
- `COBOL-BANKING/bin/` — Compiled binaries generated by build.sh

### Python (6 core modules + API + LLM + tests)

- `python/bridge.py` — COBOL subprocess execution + DAT file I/O + SQLite sync
- `python/integrity.py` — SHA-256 hash chain + HMAC verification
- `python/settlement.py` — 3-step inter-bank settlement coordinator
- `python/cross_verify.py` — Cross-node integrity verification + tamper detection
- `python/simulator.py` — Multi-day banking simulation engine
- `python/cli.py` — Command-line interface (seed, transact, verify, simulate)
- `python/auth.py` — RBAC (4 roles, 16 permissions)
- `python/api/app.py` — FastAPI application factory + static mounts
- `python/api/routes_banking.py` — Account, transaction, chain, settlement endpoints
- `python/api/routes_simulation.py` — Simulation control + SSE streaming + tamper demo
- `python/api/routes_codegen.py` — COBOL parse/generate/edit/validate endpoints
- `python/api/routes_chat.py` — LLM chat with tool-use resolution
- `python/api/routes_health.py` — System health check
- `python/llm/tools.py` — 12 tool definitions (Anthropic-compatible schema)
- `python/llm/tool_executor.py` — RBAC-gated dispatch to bridge/codegen
- `python/llm/providers.py` — Ollama (local) + Anthropic (cloud) providers
- `python/llm/conversation.py` — Session management + tool-use loop
- `python/llm/audit.py` — SQLite audit log for all tool invocations
- `python/README.md` — Python layer overview, module table, quick start
- `python/api/README.md` — REST API endpoint reference, auth model, curl examples
- `python/llm/README.md` — LLM tool catalog, provider comparison, audit schema
- `python/cobol_codegen/README.md` — Pipeline diagram, template catalog, usage examples
- `python/tests/` — 280 tests across 13 test files

### Web Console (static HTML/CSS/JS, no Node.js)

- `console/index.html` — SPA shell with nav tabs (Dashboard/Chat), role selector, health dot
- `console/css/variables.css` — Design tokens, glass morphism recipe, per-bank colors
- `console/css/layout.css` — Reset, body background, nav bar, grid layouts
- `console/css/components.css` — Glass cards, buttons, badges, toggles, feed, toast
- `console/css/dashboard.css` — Simulation controls, network graph, event feed, COBOL viewer
- `console/css/chat.css` — Chat sidebar, message bubbles, tool call cards, input bar
- `console/js/utils.js` — formatCurrency, escapeHtml, showToast, bankColor
- `console/js/api-client.js` — Fetch wrapper with X-User/X-Role headers, SSE factory
- `console/js/app.js` — SPA router (view switching), health check polling, module init
- `console/js/network-graph.js` — SVG hub-and-spoke with 6 nodes, edge flash animations
- `console/js/cobol-viewer.js` — COBOL source display with syntax highlighting
- `console/js/dashboard.js` — Simulation controls, event feed, tamper demo, verify all
- `console/js/chat.js` — LLM chatbot with provider switching and session management

### Documentation

- `docs/ARCHITECTURE.md` — Full system topology, data flow, integrity model
- `docs/GLOSSARY.md` — COBOL, banking, and project terminology
- `docs/TEACHING_GUIDE.md` — Instructor's manual (8 structured lessons)
- `docs/LEARNING_PATH.md` — Student self-study guide with exercises
- `docs/archive/` — Original specification and handoff documents

### Data (6 node directories, gitignored)

- `COBOL-BANKING/data/BANK_A/` — 8 customer accounts (ACCOUNTS.DAT, TRANSACT.DAT, bank_a.db)
- `COBOL-BANKING/data/BANK_B/` — 7 customer accounts
- `COBOL-BANKING/data/BANK_C/` — 8 customer accounts (TAMPER TARGET for demo)
- `COBOL-BANKING/data/BANK_D/` — 6 customer accounts
- `COBOL-BANKING/data/BANK_E/` — 8 customer accounts
- `COBOL-BANKING/data/CLEARING/` — 5 nostro accounts (NST-BANK-A through NST-BANK-E)

### Scripts

- `scripts/build.sh` — Compile COBOL (from COBOL-BANKING/src/, `-I COBOL-BANKING/copybooks`)
- `scripts/seed.sh` — Seed all 6 nodes with demo accounts + batches
- `scripts/prove.sh` — Full end-to-end demo (compile → seed → settle → verify → tamper → detect)

---

## Data Reference

### Account Counts

| Node | Accounts | Type | Details |
|------|----------|------|---------|
| BANK_A | 8 | Customer | ACT-A-001 through ACT-A-008 |
| BANK_B | 7 | Customer | ACT-B-001 through ACT-B-007 |
| BANK_C | 8 | Customer | ACT-C-001 through ACT-C-008 (TAMPER TARGET) |
| BANK_D | 6 | Customer | ACT-D-001 through ACT-D-006 |
| BANK_E | 8 | Customer | ACT-E-001 through ACT-E-008 |
| CLEARING | 5 | Nostro | NST-BANK-A through NST-BANK-E |
| **TOTAL** | **42** | | 37 customer + 5 nostro |

### Record Formats

**ACCTREC (Account)**: 70 bytes
- ACCT-ID (10) | ACCT-NAME (30) | ACCT-TYPE (1) | ACCT-BALANCE (12, implied decimal) | ACCT-STATUS (1) | ACCT-OPEN-DATE (8) | ACCT-LAST-ACTIVITY (8)

**TRANSREC (Transaction)**: 103 bytes
- TRANS-ID (12) | TRANS-ACCT-ID (10) | TRANS-TYPE (1) | TRANS-AMOUNT (12, implied decimal) | TRANS-DATE (8) | TRANS-TIME (6) | TRANS-DESC (40) | TRANS-STATUS (2) | TRANS-BATCH-ID (12)

**BATCH-INPUT**: Pipe-delimited
- Format: `ACCOUNT_ID|TYPE|AMOUNT|DESCRIPTION` (D/W/I/F)
- Format: `ACCOUNT_ID|T|AMOUNT|DESCRIPTION|TARGET_ID` (transfers only)

---

## Verification

```bash
# Run all 280 tests
python -m pytest python/tests/ -v

# Full end-to-end proof
./scripts/prove.sh

# Compile COBOL (optional — falls back to Mode B)
./scripts/build.sh
```

---

## Troubleshooting

**Q: Where do I start?**
A: Read `COBOL-BANKING/src/SMOKETEST.cob` — the simplest program, teaches all 4 COBOL divisions. Then follow the [Learning Path](docs/LEARNING_PATH.md).

**Q: What if cobc isn't installed?**
A: The system works without COBOL installed. build.sh skips compilation, seed.sh uses Python file I/O (Mode B). All 274 tests pass in Mode B.

**Q: Why per-node databases?**
A: Financial systems are distributed. Each node operates independently. Per-node isolation mirrors real banking — distributed, independent, reconciled through settlement.
